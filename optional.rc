DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

SUDO_PROMPT='gimme the magic juice'

MODULES="aliases.rc"

for MODULE in $MODULES ; do
    echo -e "Loading $MODULE... \c"
    . "$DIR/$MODULE" && echo "OK"
done
